<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
<link href='https://fonts.googleapis.com/css?family=Roboto' rel='stylesheet' type='text/css'>
  </head>
  <body>

  </body>
</html>

<section class="questions-show">




<br>
<h1 class="question-titles">
  <form id="downvote">

    <input type="hidden" name="question_id" value="<%= question.escape("id") %>">
    <input type="hidden" name="value" value="<%= false %>">

    <input class="dvote" id="downvote" type="image" src="arrow-down.svg" alt="Submit">
  </form>
  <form id="upvote">
    <input type="hidden" name="question_id" value="<%= question.escape("id") %>">
    <input type="hidden" name="value" value="<%= true %>">

    <input class="uvote" type="image" src="arrow-up.svg" alt="Submit">
  </form>

<em class="votes"> <%= question.escape("votes")%> </em>
<%= question.escape("title") %>
</h1>
<br>
<br>
<h2> <%= question.escape("body") %> </h2>
<br>
<% if (question.escape("filepicker_url").slice(0,5) === "https") { %>
<img src="<%= question.escape("filepicker_url") %>"> </img>
<% } %>

  <i class= "answers">


  <h1>Answers:</h1>
  <i class= "answers">
    <% question.answers().each( function(answer) {%>
      <% if (answer.escape("link_url").length > 0) { %>
    <a class=answer_link href="<%=answer.escape("link_url")%>"> <%= answer.escape("body") %> </a><br>
      <% } else { %>
      <p>  <%=answer.escape("body")%> </p><br>
        <%} %>
    <% }); %>

    <h2> Comments: </h2>
      <% question.comments().each( function(comment) {%>
      <%= comment.escape("body") %> <br>
      <% }); %>
  </i>
<br>
<p></p>
<div class="answerForm"> </div>
<div class="commentForm"></div>
</section>
